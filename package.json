{
  "name": "shure-fund",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix src/web-app run dev",
    "build": "npm --prefix src/web-app run build",
    "start": "npm --prefix src/web-app run start"
  }
}
