{
  "name": "shure-fund",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix src/web-app run dev",
    "build": "rm -rf .next && npm --prefix src/web-app run build && cp -R src/web-app/.next .next",
    "start": "npm --prefix src/web-app run start"
  }
}
